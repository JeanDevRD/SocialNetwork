@using SocialNetwork.Core.Application.ViewModels.Comment
@model CreateCommentViewModel

@{
    ViewData["Title"] = "Responder Comentario";
}

<div class="col-12 col-md-6 offset-md-3 mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-info text-white text-center">
            <h5>Responder comentario</h5>
        </div>

        <div class="card-body">
            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <p>@ViewBag.Error</p>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            }

            <form asp-action="CreateReply" asp-controller="Comment" method="post">
                <input type="hidden" asp-for="PostId" />
                <input type="hidden" asp-for="ParentCommentId" />

                <div class="mb-3">
                    <label asp-for="Content" class="form-label fw-semibold"></label>
                    <textarea asp-for="Content" rows="3" class="form-control"></textarea>
                    <span asp-validation-for="Content" class="text-danger small"></span>
                </div>

                <div class="d-flex justify-content-between">
                    <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
                    <button type="submit" class="btn btn-info text-white">Responder</button>
                </div>
            </form>

        </div>
    </div>
</div>
